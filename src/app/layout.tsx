import Image from 'next/image';
import './globals.css';
import { Open_Sans } from 'next/font/google';

import DirectAccess from '@/app/Components/DirectAccess/DirectAccess';
import { getClientNames } from '@/app/services/get-clients-names.service';

export const metadata = {
  title: 'SocialMedia1989',
  description: 'Generated by create next app',
};
const openSans = Open_Sans({
  subsets: ['cyrillic', 'latin'],
  weight: ['300', '400'],
});
export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body
        className={`${openSans.className} grid-body-wrapper max-w-screen max-h-screen`}
      >
        <header className="flex  min-h-sreen items-start justify-start icons-area relative z-20 p-4">
          <nav className="flex flex-col  gap-4">
            <DirectAccess route="/clients" name="Clientes" />

            <DirectAccess route="/about" name="Nosotros" />

            <DirectAccess route="/contact" name="Contactanos" />
          </nav>
        </header>
        <Image fill alt="bg" src={'/img0.jpg'} />
        {children}
      </body>
    </html>
  );
}
