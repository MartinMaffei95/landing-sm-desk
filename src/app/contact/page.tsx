import Folder from '@/app/Components/Folder/Folder';

import React from 'react';

const page = () => {
  return (
    <main className="folder-area flex min-h-screen relative z-20 p-4">
      <Folder folderName="Contacto">CONTACTO</Folder>
    </main>
  );
};

export default page;
